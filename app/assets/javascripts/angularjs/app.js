'use strict';

/* App Module */
//user_id = App.Entry.userId()
//user_path = App.Entry.userPath()

//angular.module('entries', []).
//  config(['$routeProvider', function($routeProvider) {
//  $routeProvider.
//      when(user_path, {controller: EntryList});//.
//      //when('/phones/:phoneId', {templateUrl: 'partials/phone-detail.html', controller: PhoneDetailCtrl}).
//      //otherwise({redirectTo: '/entries'});
//      //otherwise({controller: EntryList});
//}]);
