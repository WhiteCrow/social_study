var auditable = $('<%= "##{@auditable.class.name.underscore}-#{@auditable.id}"%>')
var urelayLink = auditable.find('.unrelay-link')
var relayedLink = auditable.find('.relayed-link')

if (urelayLink.length > 0){
  auditable.find('.unrelay-link').replaceWith('<%= relayed_link(@statable) %>')
}else if(relayedLink.length > 0){
  auditable.find('.relayed-link').replaceWith('<%= unrelay_link(@statable) %>')
}

$(auditable).find('.oprate-mic').children('.hide').show()
