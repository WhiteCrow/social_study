var title = '<%= @entry.title %>'
if (!App.Entry.previousIsContain(title)){
  $('#entry-previous #remove-unactive-li').before('<%= j previous_entry_li(@entry).to_s %>');
}
$('<%= j "#entry-tab-#{@entry.id}"%>').click()
